import SwiftUI
import SpaceUI

// SpaceUIDemoApp.swift
// A simple demo app using some SpaceUI componenets

struct SpaceUIDemo: View {
    var body: some View {
        VStack {
            
        }
        .spaceBackground()
    }
}
